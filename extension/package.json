{
  "name": "hanja-hover-extension",
  "private": true,
  "type": "module",
  "description": "Marks extension/*.js as ES modules so Node (tests) can import lookup.js. Chrome ignores this file; background.js is loaded as a module via manifest.json background.type."
}
